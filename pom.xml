<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.jenkins-ci.plugins</groupId>
        <artifactId>plugin</artifactId>
        <version>6.2236.v12dd4c483242</version>
    </parent>
    <properties>
        <!-- Incrementals (JEP-229) versioning: releases become 2.<git-count>.v<hash>, snapshots 2.999999-SNAPSHOT. -->
        <revision>2</revision>
        <changelist>999999-SNAPSHOT</changelist>
        <jenkins.version>2.541.3</jenkins.version>
        <gitHubRepo>jenkinsci/testingbot-plugin</gitHubRepo>
        <hudsonTags>buildwrapper</hudsonTags>
        <!--
          The exact set of third-party jars bundled into the .hpi. Any drift must be deliberate:
          bundling a library that a Jenkins *-api plugin already provides puts duplicate classes on the
          classpath, while dropping one that no declared plugin dependency provides causes
          NoClassDefFoundError at runtime. Strict mode turns drift into a build failure; the runtime
          half of the same invariant is checked by PluginLinkageIT.
        -->
        <hpi.bundledArtifacts>TestingBotTunnel,base64,gson,httpclient,testingbotrest</hpi.bundledArtifacts>
        <hpi.strictBundledArtifacts>true</hpi.strictBundledArtifacts>
    </properties>
    <scm>
        <connection>scm:git:https://github.com/${gitHubRepo}.git</connection>
        <developerConnection>scm:git:https://github.com/${gitHubRepo}.git</developerConnection>
        <tag>${scmTag}</tag>
        <url>https://github.com/${gitHubRepo}</url>
    </scm>
    <url>https://github.com/${gitHubRepo}</url>
    <developers>
        <developer>
            <id>testingbot</id>
            <name>Jochen Delabie</name>
            <email>info@testingbot.com</email>
        </developer>
    </developers>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!--
                      The com.testingbot:TestingBotTunnel artifact is an uber-jar that bundles an
                      old Jetty, which clashes with the Jenkins test harness' Jetty 12 on the flat
                      test classpath. That breaks any JenkinsRule-based test, including the
                      parent-injected one, so it is excluded here. See README (Known limitations).
                    -->
                    <excludes>
                        <exclude>**/InjectedTest.java</exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <!--
                  Runs PluginLinkageIT, which inspects the packaged .hpi. Because InjectedTest (and
                  with it any JenkinsRule coverage) is excluded above, nothing else ever loads this
                  plugin under a real plugin classloader; that integration test is the only guard
                  against a library that resolves on Maven's flat test classpath but is missing from
                  the plugin's Plugin-Dependencies at runtime.
                -->
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <licenses>
        <license>
            <name>The MIT License</name>
            <url>http://opensource.org/licenses/mit-license</url>
            <distribution>repo</distribution>
        </license>
    </licenses>
    <name>Jenkins TestingBot plugin</name>
    <description>This plugin integrates videos/screenshots of your TestingBot.com Selenium tests</description>
    <groupId>testingbot</groupId>
    <artifactId>testingbot</artifactId>
    <version>${revision}.${changelist}</version>
    <packaging>hpi</packaging>
    <repositories>
        <repository>
            <id>repo.jenkins-ci.org</id>
            <url>https://repo.jenkins-ci.org/public/</url>
        </repository>
    </repositories>
    <pluginRepositories>
        <pluginRepository>
            <id>repo.jenkins-ci.org</id>
            <url>https://repo.jenkins-ci.org/public/</url>
        </pluginRepository>
    </pluginRepositories>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.jenkins.tools.bom</groupId>
                <artifactId>bom-2.541.x</artifactId>
                <version>7002.v028a_3607ddc8</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>com.testingbot</groupId>
            <artifactId>TestingBotTunnel</artifactId>
            <version>4.9</version>
            <scope>compile</scope>
            <!--
              Use the self-contained "shaded" classifier and exclude all transitive dependencies.
              The tunnel embeds its own server stack (Jetty 11, Jackson, Apache HTTP, commons-cli),
              which conflicts with the versions Jenkins manages (e.g. Jetty 12, Jackson 2.22). Those
              libraries are only used internally by the tunnel at runtime, where the plugin classloader
              keeps them isolated from Jenkins core, so we consume the bundled jar rather than leak an
              unreconcilable transitive tree into the plugin build.
            -->
            <classifier>shaded</classifier>
            <exclusions>
                <exclusion>
                    <groupId>*</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.testingbot</groupId>
            <artifactId>testingbotrest</artifactId>
            <version>1.1.0</version>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.httpcomponents</groupId>
                    <artifactId>httpmime</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.json</groupId>
                    <artifactId>json</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.httpcomponents</groupId>
                    <artifactId>httpcore</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.google.errorprone</groupId>
                    <artifactId>error_prone_annotations</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.27.7</version>
            <scope>test</scope>
        </dependency>

        <!--
          testingbotrest uses org.json at runtime. The raw org.json:json jar is excluded above so the
          hpi does not bundle a second copy of a class Jenkins already ships; the classes come from the
          json-api plugin instead. That plugin must be a *direct* dependency here so it is written into
          this plugin's Plugin-Dependencies manifest entry — reaching it transitively through another
          plugin does not put org.json on this plugin's classloader and yields
          NoClassDefFoundError: org/json/JSONException at runtime.
        -->
        <dependency>
            <groupId>io.jenkins.plugins</groupId>
            <artifactId>json-api</artifactId>
        </dependency>

        <!--
          Same reasoning for Apache HttpComponents. httpcore and httpmime are excluded from
          testingbotrest above, so only httpclient is bundled; the core packages (org.apache.http and
          its .entity/.message/.util/.entity.mime subpackages) come from this plugin. It was previously
          reachable only transitively via workflow-basic-steps — declared directly so the linkage does
          not depend on another plugin's dependency list. Enforced by PluginLinkageIT.
        -->
        <dependency>
            <groupId>org.jenkins-ci.plugins</groupId>
            <artifactId>apache-httpcomponents-client-4-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.jenkins-ci.plugins</groupId>
            <artifactId>credentials</artifactId>
        </dependency>

        <dependency>
            <groupId>org.jenkins-ci.plugins</groupId>
            <artifactId>junit</artifactId>
        </dependency>


        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-step-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-cps</artifactId>
        </dependency>

        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-job</artifactId>
        </dependency>

        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-basic-steps</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.httpcomponents</groupId>
                    <artifactId>httpclient</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-durable-task-step</artifactId>
        </dependency>

        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-support</artifactId>
        </dependency>

        <dependency>
            <groupId>org.jenkins-ci.plugins</groupId>
            <artifactId>structs</artifactId>
        </dependency>

        <!-- Publish TestingBot results as a GitHub check. checks-api is the generic API; the actual
             GitHub delivery is provided at runtime by the github-checks plugin (+ a GitHub App). When
             that implementation is absent, publishing is a safe no-op (NullChecksPublisher). -->
        <dependency>
            <groupId>io.jenkins.plugins</groupId>
            <artifactId>checks-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jenkins-ci.plugins</groupId>
            <artifactId>display-url-api</artifactId>
        </dependency>
    </dependencies>
</project>
